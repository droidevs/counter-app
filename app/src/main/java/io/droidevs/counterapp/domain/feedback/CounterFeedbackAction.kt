package io.droidevs.counterapp.domain.feedback

/** Unified feedback actions for counter operations (sound + vibration). */
enum class CounterFeedbackAction {
    INCREMENT,
    DECREMENT,
    RESET
}

